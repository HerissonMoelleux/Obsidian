# Web Concepts Map

> Фундамент: как работает браузер, сеть и веб-платформа.

---

## 🎯 Почему эта папка существует?

React, TypeScript, любой фреймворк — это **абстракции над веб-платформой**. Без понимания того, что происходит "под капотом", ты будешь:
- Не понимать, почему CORS блокирует запросы
- Не знать, как работают cookies и localStorage
- Не понимать, почему приложение "тормозит"

**Аналогия:** Это как физика для инженера — можно строить без неё, но рано или поздно всё рухнет.

---

## 📁 Structure

```
01-Web Concepts/
├── Browser/
│   ├── DOM.md                    # Что такое DOM, как JS взаимодействует
│   ├── BOM.md                    # window, navigator, location objects
│   ├── Event System.md           # Bubbling, capturing, delegation
│   ├── Storage APIs.md           # localStorage, sessionStorage, IndexedDB
│   └── DevTools.md               # Как дебажить в браузере
│
├── Network/
│   ├── HTTP Basics.md            # Methods, status codes, headers
│   ├── REST API.md               # Принципы RESTful
│   ├── CORS.md                   # Почему блокирует, как решать
│   ├── Cookies.md                # Механизм, атрибуты безопасности
│   └── WebSockets.md             # Real-time коммуникация
│
└── Performance/
    ├── Core Web Vitals.md        # LCP, FID, CLS — метрики Google
    ├── Critical Rendering Path.md # Как браузер рендерит страницу
    └── Caching.md                # Browser cache, Cache-Control
```

### Когда добавлять заметки сюда?

- Столкнулся с CORS ошибкой → разобрался → записал в `CORS.md`
- Читал про Event Loop → записал в `Browser/`
- Оптимизировал загрузку → записал в `Performance/`

---

## 🌐 Browser

Как браузер превращает код в то, что видит пользователь.

### Key Topics
- [[DOM]] — Document Object Model, дерево элементов
- [[BOM]] — Browser Object Model (window, navigator, location)
- [[Event System]] — события, bubbling, capturing, delegation
- [[Storage APIs]] — localStorage, sessionStorage, IndexedDB
- [[DevTools]] — инструменты отладки

### Interview Questions
- Что такое DOM и как он связан с HTML?
- Разница между `event.target` и `event.currentTarget`?
- Когда использовать localStorage vs sessionStorage?

---

## 🌍 Network

Как браузер общается с сервером.

### Key Topics
- [[HTTP Basics]] — методы, статус-коды, заголовки
- [[REST API]] — принципы RESTful архитектуры
- [[CORS]] — Cross-Origin Resource Sharing, почему блокирует запросы
- [[Cookies]] — как работают, атрибуты (HttpOnly, Secure, SameSite)
- [[WebSockets]] — real-time коммуникация

### Interview Questions
- Разница между GET и POST?
- Что такое CORS и как его "починить"?
- Зачем нужен атрибут `HttpOnly` у cookies?

### 🔐 Security Angle
> Твой cybersec background здесь особенно полезен:
> - CORS как защита от CSRF
> - Cookie security attributes
> - HTTPS и Man-in-the-Middle

---

## ⚡ Performance

Как сделать веб-приложение быстрым.

### Key Topics
- [[Core Web Vitals]] — LCP, FID, CLS (метрики Google)
- [[Lazy Loading]] — отложенная загрузка изображений и компонентов
- [[Caching Strategies]] — браузерный кеш, Cache-Control
- [[Critical Rendering Path]] — как браузер рендерит страницу

### Interview Questions
- Что такое Core Web Vitals и зачем они нужны?
- Как оптимизировать LCP (Largest Contentful Paint)?

---

## 🔗 Connections

- [[_JavaScript Map]] — JS работает в контексте браузера
- [[_React Map]] — React абстрагирует DOM, но понимание важно
- [[_Security Map]] — сетевые концепции критичны для безопасности

---

## 📈 Progress

- [ ] DOM basics
- [ ] Event delegation
- [ ] HTTP methods & status codes
- [ ] CORS понимание
- [ ] Core Web Vitals

---

*Part of [[_Frontend Development Map]]*
