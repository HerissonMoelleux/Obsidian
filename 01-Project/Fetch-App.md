# Post Viewer & Creator App

> Клиент-серверное приложение для просмотра и создания постов блога с интеграцией REST API

**Статус:** ✅ Завершён  
**Дата:** Январь 2026  

---

## 📸 Превью

![[Fetch-App Screen.png|600]]

---

## 🎯 Цели Проекта

### Что хотел изучить
- [ ] Работа с реальными REST API (GET, POST запросы)
- [ ] Паттерны async/await на практике
- [ ] Полноценная обработка ошибок
- [ ] Манипуляция DOM и работа с формами
- [ ] Основы архитектуры клиент-сервер

### Что реально узнал
- ✅ Защитное программирование (валидировать всё!)
- ✅ Распространение ошибок в async функциях
- ✅ UX-мышление (отображение ошибок, обратная связь)
- ✅ Настройка локального API сервера (json-server)
- ✅ Promise chaining для зависимых запросов
- ✅ Валидация структуры данных

---

## 🏗️ Архитектура

### Стек технологий
- **Frontend:** Vanilla JavaScript (ES6+), HTML5, CSS3
- **API:** JSON Server (локальный REST API)
- **Паттерны:** MVC-подобное разделение, async/await

### Структура проекта
```
project/
├── index.html          # Структура UI
├── style.css           # Стили
├── app.js              # Основная логика
├── db.json             # База данных
└── README.md           # Этот файл
```

---

## 🚀 Функционал

### Основные возможности
- ✅ **Поиск постов по ID** - получение и отображение отдельных постов
- ✅ **Поиск автора** - автоматическая загрузка данных автора поста
- ✅ **Создание новых постов** - POST запрос для добавления постов
- ✅ **Обработка ошибок** - понятные сообщения об ошибках для всех случаев
- ✅ **Валидация форм** - проверка ввода перед API запросами

### Покрытие обработки ошибок
- Валидация ввода (проверка типов, диапазонов)
- Сетевые ошибки (сервер недоступен)
- HTTP ошибки (404, 500 и т.д.)
- Ошибки парсинга JSON (некорректный ответ)
- Валидация структуры данных (отсутствующие поля)

---

## 💻 Ключевые Решения в Коде

### 1. Многослойная обработка ошибок

**Проблема:** API может сломаться множеством способов - нужно отловить все случаи

**Решение:** Многоуровневая валидация и try/catch блоки
```javascript
async function getData(id, type) {
  // Слой 1: Валидация ввода
  if (typeof id !== "number" || id <= 0) {
    throw new Error("Invalid post ID");
  }

  // Слой 2: Обработка сетевых ошибок
  let res;
  try {
    res = await fetch(`http://localhost:3000/${type}s/${id}`);
  } catch (error) {
    throw new Error("Network request failed");
  }

  // Слой 3: Проверка HTTP статуса
  if (!res.ok) {
    throw new Error(`HTTP ${res.status}: ${res.statusText}`);
  }

  // Слой 4: Парсинг JSON
  let data;
  try {
    data = await res.json();
  } catch (error) {
    throw new Error("Invalid JSON response");
  }

  // Слой 5: Валидация структуры данных
  if (type === "post" && (!data.title || !data.body)) {
    throw new Error("Invalid post data structure");
  }

  return data;
}
```

**Главное:** Защитное программирование - предполагай, что всё может сломаться!

---

### 2. Promise Chaining для зависимых запросов

**Проблема:** Нужно загрузить пользователя ПОСЛЕ получения поста (userId берём из данных поста)

**Решение:** Цепочка промисов с распространением ошибок
```javascript
getData(postId, "post")
  .then((post) => {
    // Сначала: отображаем данные поста
    title.textContent = post.title;
    text.textContent = post.body;

    // Затем: загружаем связанного пользователя
    return getData(post.userId, "user");
  })
  .then((user) => {
    // Наконец: отображаем автора
    author.textContent = user.name;
  })
  .catch((err) => {
    // Один catch обрабатывает ВСЕ ошибки в цепочке
    showError(err.message);
  });
```

**Главное:** Один `.catch()` в конце обрабатывает ошибки из всей цепочки!

---

### 3. Универсальная функция с типами

**Проблема:** Нужна похожая логика для разных типов ресурсов (посты, пользователи)

**Решение:** Общая функция с параметром типа
```javascript
async function getData(id, type) {
  const endpoint = type === "post" ? "posts" : "users";
  const res = await fetch(`http://localhost:3000/${endpoint}/${id}`);
  // ... логика валидации
}

// Использование:
await getData(1, "post");   // Загружает пост #1
await getData(5, "user");   // Загружает пользователя #5
```

**Главное:** DRY принцип - одна функция, много сценариев использования!

---

## 🐛 Челленджи и Решения

### Челлендж 1: Понимание распространения ошибок

**Проблема:** Путался, когда использовать `try/catch`, а когда `.catch()`

**Что узнал:**
- `throw new Error()` внутри async функции → ловится caller's try/catch ИЛИ .catch()
- Ошибки "всплывают" через цепочки промисов
- Один catch может обработать несколько точек отказа

**Пример:**
```javascript
// Внутри getData():
if (!res.ok) {
  throw new Error(`HTTP ${res.status}`); // ← Выбрасывается здесь
}

// В обработчике событий:
getData(id, "post").catch((err) => {
  showError(err.message); // ← Ловится здесь!
});
```

---

### Челлендж 2: Обработка зависимых async операций

**Проблема:** Нужно загрузить пользователя ПОСЛЕ поста (нельзя параллельно)

**Решение:** Promise chaining - возвращать промис из .then()

**Неправильный подход (пробовал сначала):**
```javascript
getData(postId, "post").then((post) => {
  getData(post.userId, "user"); // ❌ Не ждёт!
});
// Загрузка пользователя происходит, но результат не используется
```

**Правильный подход:**
```javascript
getData(postId, "post")
  .then((post) => {
    return getData(post.userId, "user"); // ✅ Возвращает промис
  })
  .then((user) => {
    // Теперь у нас есть пользователь!
  });
```

---

### Челлендж 3: Поведение submit у формы по умолчанию

**Проблема:** Страница перезагружалась при отправке формы

**Решение:** `e.preventDefault()`
```javascript
inputForm.addEventListener("submit", (e) => {
  e.preventDefault(); // ← Предотвращает перезагрузку страницы!
  // ... логика fetch
});
```

**Главное:** Формы имеют дефолтное поведение submit, которое нужно отключать для SPA!

---

## 📊 Что Получилось Хорошо

### Сильные стороны
- ✅ **Обработка ошибок** - систематически покрыл все случаи отказа
- ✅ **User experience** - понятные сообщения об ошибках, обратная связь
- ✅ **Организация кода** - разделение ответственности (данные, UI, события)
- ✅ **Реальная интеграция API** - работа с настоящим HTTP сервером
- ✅ **Problem-solving** - самостоятельная отладка проблем

---

## 🎓 Ключевые Выводы

### Технические знания
1. **Обработка ошибок многослойная** - валидация ввода, проверка сети, HTTP статус, парсинг JSON, валидация данных
2. **Async операции требуют продуманной последовательности** - chaining для зависимых операций, Promise.all() для независимых
3. **UX важен в сценариях ошибок** - не просто логировать в консоль, показывать пользователю что случилось
4. **Конвертация типов критична** - `Number(input.value)` перед использованием как ID

### Мышление разработчика
1. **Начинай с пользовательского ввода** - что может пойти не так? Валидируй рано!
2. **Думай о режимах отказа** - сеть упала? Ошибка сервера? Некорректные данные?
3. **Разделяй ответственность** - получение данных ≠ обновление UI ≠ обработка событий
4. **Тестируй граничные случаи** - невалидные ID, отсутствующие поля, ошибки сервера

---

## 🚀 Запуск Проекта

### Предварительные требования
```bash
npm install -g json-server
```

### Настройка
1. Клонируй/скачай файлы проекта
2. Запусти API сервер:
```bash
json-server --watch db.json --port 3000
```
3. Открой `index.html` в браузере

---

## 💭 Рефлексия

### Чем горжусь
Написал полноценную обработку ошибок без подглядывания в туториалы. Продумал каждую точку отказа систематически и покрыл их все. Это показывает, что я думаю как профессиональный разработчик, а не просто кодирую "счастливый путь".

### Самая сложная часть
Понимание promise chaining для зависимых запросов. Кликнуло, когда я понял, что `.then()` возвращает новый промис, поэтому возврат промиса изнутри `.then()` позволяет добавить ещё один `.then()`.

### Aha-момент
Каскад обработки ошибок - от валидации ввода → сеть → HTTP → парсинг → структура данных. Каждый слой защищает от разных режимов отказа. Это и есть "защитное программирование" в действии!

### Архитектурные ошибки

В функции `getData()` есть мнимая универсальность, хотя на самом деле при расширении и усложнении проекта, функция будет использоваться неправильно, для решения этой проблемы используются кастомные "hooks".
Также есть проблема в обработке ошибок.

---

## 📝 Заметки Будущему Себе

### Что помнить
- Всегда валидируй пользовательский ввод ПЕРЕД API запросами
- `e.preventDefault()` обязателен для обработки форм
- Сообщения об ошибках должны быть понятны пользователю, а не разработчику
- Разделяй ответственность: слой данных, слой UI, слой событий
- Конвертация типов важна: `Number(input.value)`

### Паттерны кода для повторного использования
- Многослойная обработка ошибок в getData()
- Promise chaining для зависимых операций
- Паттерн функции отображения ошибок
- Универсальные функции на основе типов

### Советы по отладке
- Network tab показывает детали запроса/ответа
- Ошибки в консоли показывают точную строку отказа
- try/catch блоки изолируют источники ошибок
- Тестируй с невалидными данными намеренно
